# Base URL of the bioaro-dashboard-api NestJS backend.
# If unset or unreachable, the app falls back to bundled mock data
# (the same Shayan / BAL-7845123 test panel used in the design review).
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1
